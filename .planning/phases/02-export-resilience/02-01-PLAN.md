---
phase: 02-export-resilience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/archive/csv/photos.ts
  - src/archive/csv/index.ts
  - src/export/orchestrator.ts
  - src/export/convert.ts
  - test/archive/csv/photos.test.ts
autonomous: true
requirements:
  - PHOTO-01

must_haves:
  truths:
    - "D-01: `csv/photos.csv` has exactly the columns `event_id,photo_id,base_url` in that order — minimal, no event context columns"
    - "D-02: Events whose `featuredEventPhoto` is null produce no row in `photos.csv` (no empty rows); the file may be absent if no events have photos"
    - "D-03: `meetup-exit convert --input <dir> --out <out>` produces a `csv/photos.csv` from `event-details.jsonl` identical in shape and content to the fresh `export` output for the same data"
    - "A completed `export` run that includes events writes `csv/photos.csv` with one row per event that has a non-null `featuredEventPhoto`"
    - "If no event has a `featuredEventPhoto`, `csv/photos.csv` is not written"
    - "`vp check` and `vp test` pass after changes"
  artifacts:
    - path: "src/archive/csv/photos.ts"
      provides: "PhotoCsvRow type and writePhotosCsv function — mirrors the pattern in src/archive/csv/events.ts"
      exports: ["PhotoCsvRow", "writePhotosCsv"]
    - path: "src/archive/csv/index.ts"
      provides: "Barrel exports for archive CSV writers — must export PhotoCsvRow and writePhotosCsv"
      contains: "writePhotosCsv"
    - path: "src/export/orchestrator.ts"
      provides: "Export flow that accumulates PhotoCsvRow during event-details loop and writes csv/photos.csv at the end"
      contains: "photoCsvRows"
    - path: "src/export/convert.ts"
      provides: "Convert flow that derives photoCsvRows from event-details.jsonl and writes csv/photos.csv"
      contains: "photoCsvRows"
    - path: "test/archive/csv/photos.test.ts"
      provides: "Unit test for writePhotosCsv following the JsonlWriter/checksums test pattern"
      contains: "writePhotosCsv"
  key_links:
    - from: "src/archive/csv/photos.ts"
      to: "src/archive/csv/write-csv.ts"
      via: "writeCsvFile"
      pattern: "writeCsvFile"
    - from: "src/archive/csv/index.ts"
      to: "src/archive/csv/photos.ts"
      via: "barrel re-export of writePhotosCsv and PhotoCsvRow"
      pattern: "from \"./photos.ts\""
    - from: "src/export/orchestrator.ts"
      to: "src/archive/csv/index.ts"
      via: "import writePhotosCsv and PhotoCsvRow"
      pattern: "writePhotosCsv"
    - from: "src/export/convert.ts"
      to: "src/archive/csv/index.ts"
      via: "import writePhotosCsv and PhotoCsvRow"
      pattern: "writePhotosCsv"
---

<objective>
Add `csv/photos.csv` to both the `export` and `convert` outputs, implementing the PHOTO-01 requirement by extracting `featuredEventPhoto` data from event-details records into a minimal three-column CSV.

Purpose: Preserve event photo metadata in a structured form so consumers can join photo URLs back to events via `event_id` after the Meetup platform is gone.
Output: New `src/archive/csv/photos.ts`, updated barrel exports, orchestrator and convert engines wired to produce photos.csv, plus a unit test for the new writer.
</objective>

<execution_context>
@/Users/juherr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juherr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-export-resilience/02-CONTEXT.md
@.planning/phases/02-export-resilience/02-RESEARCH.md

@src/archive/csv/events.ts
@src/archive/csv/groups.ts
@src/archive/csv/index.ts
@src/archive/csv/write-csv.ts
@src/export/orchestrator.ts
@src/export/convert.ts
@src/meetup/functions/get-event-details.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PhotoCsvRow type, writePhotosCsv function, and unit test</name>
  <files>
    src/archive/csv/photos.ts
    src/archive/csv/index.ts
    test/archive/csv/photos.test.ts
  </files>
  <read_first>
    src/archive/csv/events.ts
    src/archive/csv/groups.ts
    src/archive/csv/index.ts
    src/archive/csv/write-csv.ts
    test/archive/jsonl-writer.test.ts
  </read_first>
  <action>
Create `src/archive/csv/photos.ts` following the exact pattern in `src/archive/csv/events.ts` (read it first to mirror style, indentation, and import order).

Required exports from `src/archive/csv/photos.ts`:
- Type `PhotoCsvRow` with fields (in this order): `eventId: string`, `photoId: string`, `baseUrl: string`.
- Async function `writePhotosCsv(filePath: string, rows: PhotoCsvRow[]): Promise<void>` that delegates to `writeCsvFile` imported from `./write-csv.ts`.

CSV column order and header names (exact strings, in this order): `event_id`, `photo_id`, `base_url`. The data row mapping must be `[r.eventId, r.photoId, r.baseUrl]`.

Update `src/archive/csv/index.ts` (barrel file) to re-export the new symbols, preserving alphabetical/insertion order used in the existing file:
- Add `export type { PhotoCsvRow } from "./photos.ts";` in the type-only block.
- Add `export { writePhotosCsv } from "./photos.ts";` in the value block.

Create `test/archive/csv/photos.test.ts` mirroring the structure of `test/archive/jsonl-writer.test.ts` (read it first). The test must:
- Use `mkdtemp` + `tmpdir` for a temporary directory, with `beforeEach`/`afterEach` cleanup.
- Cover at minimum: writing a file with two rows produces a CSV whose first line is exactly `event_id,photo_id,base_url` and whose subsequent lines contain the row data in order; writing an empty array still produces a header-only CSV (the writer itself does not guard against empty arrays — the orchestrator does that).
- Use `readFile(filePath, "utf-8")` then `content.trimEnd().split("\n")` to assert on line count and content.
- Import `writePhotosCsv` and `PhotoCsvRow` from `../../../src/archive/csv/photos.ts` (mirror existing test relative paths — verify by reading `test/archive/jsonl-writer.test.ts`).

No `any` types anywhere. Follow the project's TypeScript strictness rules from `.planning/codebase/CONVENTIONS.md`.
  </action>
  <verify>
Run `vp check` — must pass with no type or lint errors.
Run `vp test test/archive/csv/photos.test.ts` — must pass.
Run `grep -n "writePhotosCsv" src/archive/csv/index.ts` — must return a non-empty match.
Run `grep -n "PhotoCsvRow" src/archive/csv/index.ts` — must return a non-empty match.
  </verify>
  <done>
`src/archive/csv/photos.ts` exists and exports `PhotoCsvRow` (fields `eventId`, `photoId`, `baseUrl`) and `writePhotosCsv`. Barrel `src/archive/csv/index.ts` re-exports both symbols. `test/archive/csv/photos.test.ts` exists and asserts header row equals `event_id,photo_id,base_url` and verifies row content. `vp check` and `vp test` pass.
  </done>
  <acceptance_criteria>
    - "D-01: `src/archive/csv/photos.ts` contains the literal string `event_id` and `photo_id` and `base_url` as column headers (minimal columns only)"
    - `src/archive/csv/photos.ts` contains `export async function writePhotosCsv(`
    - `src/archive/csv/photos.ts` contains `export type PhotoCsvRow`
    - `src/archive/csv/index.ts` contains `from "./photos.ts"` (two times: one for the type, one for the function)
    - `test/archive/csv/photos.test.ts` exists and `vp test test/archive/csv/photos.test.ts` exits 0
    - `vp check` exits 0
    - `grep -n "any" src/archive/csv/photos.ts` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire writePhotosCsv into runExport (orchestrator)</name>
  <files>
    src/export/orchestrator.ts
  </files>
  <read_first>
    src/export/orchestrator.ts
    src/archive/csv/photos.ts
    src/archive/csv/index.ts
    src/meetup/functions/get-event-details.ts
  </read_first>
  <action>
Modify `src/export/orchestrator.ts` to accumulate `PhotoCsvRow` entries during the event-details loop and write `csv/photos.csv` alongside the other CSV outputs.

Imports to add at the top of the file (preserve existing import order — values together, types together, per `.planning/codebase/CONVENTIONS.md` Import Organization):
- Add `writePhotosCsv` to the existing value import from `../archive/csv/index.ts` (the block that imports `writeGroupsCsv`, `writeEventsCsv`, etc.).
- Add `PhotoCsvRow` to the existing type import from `../archive/csv/index.ts` (the block that imports `GroupCsvRow`, `EventCsvRow`, etc.).

Add a new in-memory array next to the existing CSV row arrays (`groupCsvRows`, `eventCsvRows`, `rsvpCsvRows`, `attendeeCsvRows`, `answerCsvRows`):
- `const photoCsvRows: PhotoCsvRow[] = [];` — placed between `eventCsvRows` and `rsvpCsvRows` to keep entity-order consistency.

Inside the event-details loop, immediately after the existing `eventCsvRows.push({ ... featuredPhotoId, featuredPhotoBaseUrl ... })` block in the `try` branch, add a conditional push: when `details.featuredEventPhoto` is not null, push `{ eventId: details.id, photoId: details.featuredEventPhoto.id, baseUrl: details.featuredEventPhoto.baseUrl }` to `photoCsvRows`. Per locked decision D-02, events with `featuredEventPhoto === null` are skipped entirely — no empty rows.

In the `if (!options.dryRun) { ... }` block that writes CSVs, add a guarded write call mirroring the existing pattern for `groupCsvRows`/`eventCsvRows`:
- `if (photoCsvRows.length > 0) await writePhotosCsv(join(options.outDir, "csv/photos.csv"), photoCsvRows);`
- Place this call immediately after the `writeEventsCsv` call to keep entity-order consistency.

In the dry-run else branch that logs `[dry-run] would write N event rows`, add an analogous log line:
- `logger.info(\`[dry-run] would write ${photoCsvRows.length} photo rows\`);` — place it immediately after the `[dry-run] would write ... event rows` line.

Do not modify the manifest, GDPR report, checksums, or any other write logic. The photo CSV is independent and joins to events via `event_id` only (D-01).

Do not introduce any `any` types. The `EventDetails` type already declares `featuredEventPhoto: { id: string; baseUrl: string } | null` per `src/meetup/functions/get-event-details.ts` — use a strict null check.
  </action>
  <verify>
Run `vp check` — must pass.
Run `grep -n "photoCsvRows" src/export/orchestrator.ts` — must return at least four matches (declaration, push, guarded write, dry-run log).
Run `grep -n "writePhotosCsv" src/export/orchestrator.ts` — must return at least two matches (import and call site).
Run `vp test` — all existing tests must still pass.
  </verify>
  <done>
`src/export/orchestrator.ts` imports `writePhotosCsv` and `PhotoCsvRow`, declares `photoCsvRows`, pushes rows only when `featuredEventPhoto !== null`, and conditionally writes `csv/photos.csv` (only when `photoCsvRows.length > 0`). Dry-run mode logs the would-write count. `vp check` and `vp test` pass.
  </done>
  <acceptance_criteria>
    - "D-02: `src/export/orchestrator.ts` contains `details.featuredEventPhoto !== null` (null featured photos skipped, no empty rows)"
    - `src/export/orchestrator.ts` contains `writePhotosCsv` (in both import and call site)
    - `src/export/orchestrator.ts` contains `PhotoCsvRow`
    - `src/export/orchestrator.ts` contains the literal `photoCsvRows: PhotoCsvRow[] = []`
    - `src/export/orchestrator.ts` contains `if (photoCsvRows.length > 0)`
    - `src/export/orchestrator.ts` contains `csv/photos.csv` in the write call site
    - `vp check` exits 0
    - `vp test` exits 0
    - `grep -c "any" src/export/orchestrator.ts` shows no new occurrences vs. baseline (count must not increase)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Derive photos.csv in runConvert from event-details.jsonl</name>
  <files>
    src/export/convert.ts
  </files>
  <read_first>
    src/export/convert.ts
    src/archive/csv/photos.ts
    src/archive/csv/index.ts
    src/export/orchestrator.ts
  </read_first>
  <action>
Modify `src/export/convert.ts` so `runConvert` also derives `csv/photos.csv` from `raw/event-details.jsonl`. Per locked decision D-03, convert output must match a fresh export run.

Imports to update at the top of the file:
- Add `writePhotosCsv` to the existing value import from `../archive/csv/index.ts`.
- Add `PhotoCsvRow` to the existing type import from `../archive/csv/index.ts`.

Add a new in-memory array next to the existing CSV row arrays:
- `const photoCsvRows: PhotoCsvRow[] = [];` — placed between `eventCsvRows` and `rsvpCsvRows`.

Inside Pass 2 (`for await (const record of readJsonlRecords(join(rawDir, "event-details.jsonl")))`), after the existing `eventCsvRows.push({ ... })` block, derive the photo row from the same JSONL record:
- The variable `photoRaw` already exists in this scope (a `Record<string, unknown> | null`). The variable `featuredEventPhoto` already exists and is either `{ id, baseUrl }` or `null`.
- If `featuredEventPhoto !== null`, push `{ eventId: id, photoId: featuredEventPhoto.id, baseUrl: featuredEventPhoto.baseUrl }` to `photoCsvRows`. Reuse the existing locally-scoped `id` and `featuredEventPhoto` variables — do not re-parse the raw record.

In the `if (!options.dryRun) { ... }` block that writes CSVs, add a guarded call mirroring the existing pattern:
- `if (photoCsvRows.length > 0) { await writePhotosCsv(join(options.outDir, "csv/photos.csv"), photoCsvRows); }`
- Place this call immediately after the `writeEventsCsv` call (same position as in the orchestrator) to keep entity-order consistency.

In the dry-run `else` branch, add `logger.info(\`[dry-run] would write ${photoCsvRows.length} photo rows\`);` immediately after the `event rows` log line.

Do not modify the existing `KNOWN_JSONL_FILES` list — photos are derived from `event-details.jsonl`, not a separate JSONL file. Do not introduce any `any` types.
  </action>
  <verify>
Run `vp check` — must pass.
Run `grep -n "photoCsvRows" src/export/convert.ts` — must return at least four matches.
Run `grep -n "writePhotosCsv" src/export/convert.ts` — must return at least two matches.
Run `vp test` — all existing tests pass.

Manual sanity check (no automated test required at this stage): construct an `event-details.jsonl` with one record whose `raw.featuredEventPhoto` is `{ id: "p1", baseUrl: "https://example/photo.jpg" }` and a second whose `raw.featuredEventPhoto` is `null`. Run `bun src/cli/main.ts convert --input <dir> --out <out>`. Confirm `<out>/csv/photos.csv` contains exactly two lines: header + one data row referencing `p1`.
  </verify>
  <done>
`src/export/convert.ts` imports `writePhotosCsv`/`PhotoCsvRow`, derives `photoCsvRows` in Pass 2 (event-details), and writes `csv/photos.csv` only when at least one event has a non-null `featuredEventPhoto`. Dry-run mode logs the count. `vp check` and `vp test` pass.
  </done>
  <acceptance_criteria>
    - "D-03: `src/export/convert.ts` derives `photos.csv` from `event-details.jsonl` (contains `writePhotosCsv` call site and `csv/photos.csv` path) — convert output matches fresh export output"
    - `src/export/convert.ts` contains `writePhotosCsv` (in both import and call site)
    - `src/export/convert.ts` contains `PhotoCsvRow`
    - `src/export/convert.ts` contains the literal `photoCsvRows: PhotoCsvRow[] = []`
    - `src/export/convert.ts` contains `featuredEventPhoto !== null`
    - `src/export/convert.ts` contains `if (photoCsvRows.length > 0)`
    - `src/export/convert.ts` contains `csv/photos.csv` in the write call site
    - `vp check` exits 0
    - `vp test` exits 0
    - `grep -c "any" src/export/convert.ts` shows no new occurrences vs. baseline
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `vp check` passes (oxlint + oxfmt + tsc) — no type errors, no lint errors.
2. `vp test` passes — including the new `test/archive/csv/photos.test.ts`.
3. The exported barrel `src/archive/csv/index.ts` re-exports `PhotoCsvRow` and `writePhotosCsv`.
4. `runExport` and `runConvert` both produce `csv/photos.csv` with header `event_id,photo_id,base_url` when at least one event has `featuredEventPhoto`.
5. No `photos.csv` is written when zero events have `featuredEventPhoto` (D-02).
6. The orchestrator's manifest, GDPR report, checksums, and other writes are unchanged.
</verification>

<success_criteria>
- PHOTO-01 satisfied: photo metadata is preserved per event in a minimal three-column CSV.
- Convert output is byte-equivalent in shape to export output for the same JSONL input (D-03).
- Established CSV pattern preserved — new writer mirrors `events.ts`/`groups.ts` exactly, barrel updated, test added.
- `vp check` + `vp test` green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-export-resilience/02-01-SUMMARY.md` following the summary template.
</output>
</output>
